spring:
  datasource:
    username: "test"
    password : "test"
    url : jdbc:mysql://localhost:3306/library
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

